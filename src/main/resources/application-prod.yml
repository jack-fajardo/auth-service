spring:
  devtools:
    restart:
      enabled: false
    livereload:
      enabled: false

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update   # (see note below)

thymeleaf:
  cache: true

logging:
  level:
    org.springframework.security: INFO
