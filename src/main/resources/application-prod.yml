spring:
  devtools:
    restart:
      enabled: false
    livereload:
      enabled: false

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update   # (see note below)

thymeleaf:
  cache: true

logging:
  level:
    root: INFO
    org.springframework: INFO
    org.springframework.security: DEBUG
